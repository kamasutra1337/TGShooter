# Production build config (loaded by `vite build`). The Telegram Mini App on
# GitHub Pages connects to the authoritative game server hosted on Railway.
VITE_SERVER_URL=wss://tgshooter-server-production.up.railway.app
